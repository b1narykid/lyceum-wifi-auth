                                   DESCRIPTION
                                  -------------

This directory contains subdirectories with platform-specific implementations
(e.g. `bash`, `curl`).

Each implementation provides at least an automatic authentication as a *guest* user.


                PORTING TO ANOTHER PLATFORM, LANGUAGE OR TOOLSET
               --------------------------------------------------

TBD
